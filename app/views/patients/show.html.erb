<h1><%= @patient.name %></h1>

<% @patient.appointments.each do |appt| %>
  <div>
    <h5><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")%>: <%= link_to appt.doctor.name, doctor_path(appt.doctor) %></h5>
  </div>
<%end%>
