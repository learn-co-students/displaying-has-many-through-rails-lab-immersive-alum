<h1><%= @doctor.name %></h1>

<h3><%= @doctor.department%></h3>

<% @doctor.appointments.each do |appt| %>
  <div>
    <h5><%= appt.appointment_datetime.strftime("%B %d, %Y at %H:%M")%>: <%= link_to appt.patient.name, patient_path(appt.patient) %></h5>
  </div>
<%end%>
