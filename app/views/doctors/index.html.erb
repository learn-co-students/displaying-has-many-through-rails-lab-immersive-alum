<h1>Doctors</h1>

<% @doctors.each do |doctor| %>
  <div>
    <span><%= link_to doctor.name, doctor_path(doctor)%></span>
    <p><%= pluralize(doctor.patients.count, "Patient")%> </p>
  </div>
<%end%>
